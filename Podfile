# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'DisplayVideos' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DisplayVideos
  pod 'Firebase/Auth'
  pod 'GoogleSignIn'
  pod 'Kingfisher'

  target 'DisplayVideosTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
